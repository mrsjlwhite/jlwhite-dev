type IExperience = {
    name: string
    time: string //`${startDate} to ${endDate}`
    title: string
    description: string
    fullDescription: string[]
    tech: string[],
    techIcons: string[]
}

export default IExperience;